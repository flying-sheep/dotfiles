[user]
	user = flying-sheep
	name = Phil Schaf
	email = flying-sheep@web.de
[core]
	editor = vim
[color]
	branch = auto
	status = auto
	diff = auto
	ui = auto
[push]
	default = tracking
[pull]
	rebase = true
[merge]
	tool = kdiff3
[diff]
	tool = kompare
[difftool]
	prompt = off
[alias]
	post-review = !post-review --guess-summary --guess-description --username=phils --password="$(ksshaskpass reviewboard)" --parent=master --tracking-branch=origin/master
	branch-empty = !git symbolic-ref HEAD refs/heads/$1 && rm .git/index && git clean -fdx
[url "git://anongit.kde.org/"]
   insteadOf = kde:
[url "ssh://git@git.kde.org/"]
   pushInsteadOf = kde:
[url "git@git.kde.org:"]
	pushInsteadOf = kde:
